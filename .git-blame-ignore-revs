# Revisions to skip in `git blame` — bulk reformatting, no behaviour change.
#
# GitHub honours this file automatically. To get the same locally:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Only add a commit here if it changes formatting and nothing else.

# style: apply ruff format to the whole repository
ae521b751f50c7046fdb81d12d48bdf89cd2e162
