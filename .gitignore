__pycache__
*.egg-info
secrets.py
dist
build
test.py
/urls.txt
*.flac
/Downloads
/StreamripDownloads
*.mp3
StreamripDownloads
*.wav
*.log
*.mp4
*.opus
*.mkv
*.aac
*.pyc
*test.py
!tests/conftest.py
/.mypy_cache
.DS_Store
pyrightconfig.json
.venv/
dev-reinstall.sh
CLAUDE.md
# Sous-produits vhs (voir demo/build_demos.sh) — les .mp4 intermédiaires
demo/*.mp4
# Sorties du banc d'essai (bench.sh) : CSV, logs et le venv upstream construit
# pour la comparaison. Le script est versionné, ses résultats non.
benchmark_runs/

# Configuration et sorties des tests en conditions réelles. Le script lui-même
# est versionné ; test_urls.env porte des URLs de compte et un ARL (= un jeton
# de session Deezer), et realworld_runs/ des logs de session. Ni l'un ni l'autre
# ne doit être ajouté, même avec -f. Modèle : test_urls.env.example.
test_urls.env
realworld_runs/
# Artefacts de couverture (générés par pytest --cov)
.coverage
coverage.json
